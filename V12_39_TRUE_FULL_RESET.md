# v12.39 전체 초기화 수정

## 원인

기존 전체 초기화는 빈 자료를 만드는 대신 `initialState()`를 호출하여 코드에 내장된 기본 품목 304개와 해당 품목의 초기 재고를 다시 생성했습니다.

또한 실제 초기화 저장 사유는 `reset-all`, `reset-inventory` 같은 형식이지만 일부 복원 및 Cloud 병합 코드는 정확히 `reset`만 초기화로 인식했습니다.

## 변경 내용

- 전체 초기화 후 품목, 거래처, 재고, 거래, 단가, 과사용, 월마감, 감사자료를 빈 상태로 저장
- 모든 `reset-*` 저장을 파괴적 변경으로 인식하여 기존 Cloud 데이터와 병합하지 않음
- 초기화 상태에서는 구버전 Master 및 로컬 복구 스냅샷을 자동 복원하지 않음
- 사용자 계정과 권한 설정은 유지
- 실행 전 JSON 백업, 2단계 확인, Cloud 전체 교체 방식은 유지
